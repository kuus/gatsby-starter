import React from "react";
import Layout from "src/components/Layout";

const PageTest = () => {
  return (
    <Layout>
      <h1>Test static page without markdown, without queries</h1>
    </Layout>
  );
};

export default PageTest;
